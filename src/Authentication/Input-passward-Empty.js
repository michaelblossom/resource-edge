import React from "react";
import FormPasswardEmpty from "./Form-Passward-Empty";

const InputPasswardEmpty = ({ placeholder }) => {
	return (
		<div>
			<FormPasswardEmpty placeholder={placeholder} />
		</div>
	);
};

export default InputPasswardEmpty;
